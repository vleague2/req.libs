<div class="col-sm-6">
  <div class="card mb-4">
    <h5 class="card-header text-center">{{this.title}}</h5>
    <div class="card-body text-center">
      <p class="card-text">{{{this.body}}}</p>
        <a href="/profile/edit/{{this.id}}" class="btn btn-info mr-2">Rename Story</a> 
        <a href="/profile/delete/{{this.id}}" class="btn btn-danger ml-2">Delete Story</a>    
    </div>
  </div>
</div>